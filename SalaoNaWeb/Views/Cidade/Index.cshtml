@model IEnumerable<SalaoNaWeb.Models.Cidade>

@{
    ViewBag.Title = "Index";
}

<h2><span>@Html.ActionLink(" Incluir", "Create", null, new { @class = "btn btn-primary glyphicon glyphicon-pencil" })</span></h2>

<table class="table table-hover table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cidNom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cidUf)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cidNom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cidUf)
            </td>
            <td>
                @Html.ActionLink(" Alterar", "Edit", new { id = item.cidId }, new { @class = "btn btn-success btn-sm glyphicon glyphicon-edit" })
                @Html.ActionLink(" Detalhes", "Details", new { id = item.cidId }, new { @class = "btn btn-success btn-sm glyphicon glyphicon-list-alt" })
                @Html.ActionLink(" Deletar", "Delete", new { id = item.cidId }, new { @class = "btn btn-danger btn-sm glyphicon glyphicon-remove-sign" })
        </tr>
    }

</table>
